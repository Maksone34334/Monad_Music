{
  "name": "pods-music-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Pods Music Playlist NFT project",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:monad": "hardhat run scripts/deploy.js --network monadTestnet",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^17.2.1",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0"
  }
}
